# White Quest 改善アイデア集

ユーザー提案の7項目に、コードベース調査で見つけた追加アイデアを加えてまとめています。各項目に「難易度」「効果」を付け、実装するときの勘所も添えました。実装は今回行わず、この後の「バイブコーディング」の参考資料として使う想定です。

## 目次

- [評価基準](#評価基準)
- [A. ユーザー提案の7項目](#a-ユーザー提案の7項目)
- [B. 追加提案](#b-追加提案)
  - [ゲーム機能](#ゲーム機能)
  - [演出・UI](#演出ui)
  - [技術・運用](#技術運用)
- [C. 進める順番の目安](#c-進める順番の目安)

---

## 評価基準

**難易度**
- 🟢 低：CSS/JSの数十行程度、1ファイル内で完結。数時間〜半日
- 🟡 中：複数ファイルにまたがる、または新しい仕組みを1つ設計する必要がある。半日〜数日
- 🔴 高：アーキテクチャ変更・外部連携・多数の作り込みが必要。数日〜

**効果**
- 🟢 低：あると嬉しいが無くても困らない、地味な改善
- 🟡 中：プレイ体験や見た目が明確に良くなる
- 🔴 高：ゲームの遊び方・届けられる相手・印象が大きく変わる

---

## A. ユーザー提案の7項目

### 1. CPU対戦機能

**難易度**: 🟡 中　**効果**: 🔴 高

現状のコードには `playMode` 変数（`1`=2人対戦、`2`=CPU対戦のつもりで用意されている）、`npcBattle()` 関数、`choiceLog.cpu`/`battleLog.cpuFirst` のメッセージ、`decideTurn()` の `'CPU'` 表示分岐など、**骨組みは既にかなり存在しています**。未着手なのは以下の2点だけです。

1. `playMode` を `2` に切り替えるUI（現状どこにもボタンが無く、常に `1` 固定）
2. CPU側の意思決定ロジックが一切無い。具体的には：
   - ダイスを止めるタイミング（`rollDice()` は「クリックされるまで待つ」設計なので、CPU用には短いディレイ後に自動で"止める"を実行する分岐が要る）
   - リロールするか（`promptReroll()` の「はい/いいえ」をCPUがランダムまたは固定ルールで選ぶ）
   - 友情コンボで誰を呼ぶか（`nakamu5Action()` のキャラ選択ダイアログをCPUが自動選択する）

前回のバグ修正で `npcBattle()` のループ条件バグ（`||`になっていた）は既に直してあるので、そこは心配不要です。まずは「常にランダム/固定ルールで判断する弱いCPU」を作り、後で強さを調整する方向がおすすめです。

### 2. デプロイ時のスマホ対応（PWA化も見据えて）

**難易度**: 🔴 高　**効果**: 🔴 高

`style.css` は `#game-display{width:720px;height:540px}` を筆頭に、ほぼ全要素が固定px・`position:absolute` で組まれています。対応方針は2択です。

- **本格レスポンシブ化**：全要素を相対単位（%, vw, flex/grid）に書き直す。見た目の作り込みが最初からやり直しに近くなるため高難易度。
- **スケール表示ラッパー**（おすすめ）：`#game-container` 全体を1つの固定サイズの「キャンバス」とみなし、外側に `transform: scale()` を当てて画面幅に合わせて縮小表示する。既存レイアウトを一切崩さずに対応でき、実装量も少ない。JSで `window.innerWidth` に応じてスケール値を計算し、リサイズ時に再計算する処理を追加するだけで済む。

PWA化には `manifest.json` とService Workerの追加が必要です。現状アイコン素材は `icon.ico` のみなので、192×192・512×512のPNGアイコンを別途用意する必要があります（既存の `img/cards/Random.png` 等を流用して簡易に作ることも可能）。オフラインキャッシュ対象は `index.html`/`script.js`/`style.css`/`img/*`/`fonts/*` で、ビルドツールが無い構成なのでService Worker自体も手書きで数十行程度になります。

### 3. シャークんの霧をフリー素材を使わず表現できるか

**難易度**: 🟢 低〜🟡 中　**効果**: 🟡 中

現状の `.cloud`/`.cloud::before`（`style.css`）は `img/effects/cloud.png` というフリー素材画像を `background-position` でスクロールさせるスプライトアニメーションです。CSSの `radial-gradient` を複数枚重ねてぼかした「もや」状の背景を作り、それを `background-position` や `transform: translateX` でゆっくり流す手法で、画像無しでも近い質感を再現できます（CSSだけで作る雲・霧エフェクトは定番テクニックです）。

素材の見た目が完全に同一にはなりませんが、「フリー素材への依存・ライセンス表記の必要性が無くなる」という実利があります。既存の `toggleCloudEffect()`（表示/非表示の切り替え関数）はそのまま使い回せます。

### 4. 霧のようなエフェクトを他キャラにもつけられるか（フリー素材を使わず）

**難易度**: 🟡 中　**効果**: 🟡〜🔴 中〜高

現状、状態異常・バフ系の演出はログのテキストだけで、見た目の変化があるのはシャークんの雲隠れ（`.cloud`）だけです。他にも見た目を付けられそうな状態がいくつもあります。

| キャラ・状態 | 演出イメージ |
|---|---|
| きりやんの火傷（`isKiriyan2`） | 赤〜オレンジの揺らめくグラデーションのオーバーレイ |
| スマイルの凍結（`isSmile2`） | 水色の結晶パターン・霜が張るようなオーバーレイ |
| Broooockの睡眠（`turnSkip`） | "Zzz..."のテキストが浮かんで消えるアニメーション |
| きんときの被ダメ蓄積中（`spValue`） | 拳のアイコンが赤く光る／振動する |
| シールド展開中（`shieldDamage > 0`） | キャラ周囲に薄い光の輪（box-shadowのパルスアニメーション） |

`toggleCloudEffect()` と同じ「該当フラグが立ったらクラスを付与、外れたら外す」パターンを一般化し、`applyStatusEffect(actor, effectName)` のような共通関数を1つ作ってから状態ごとのCSSを追加していくと管理しやすくなります。

### 5. 左サイドメニュー（オプション・デバッグ用ダイス確定機能）

**難易度**: 🟢 低〜🟡 中　**効果**: 🟡 中

現状の `#debug` パネルは `position:absolute;left:0;top:0` で常時表示されており、つい先ほどプレイヤー/敵のダイス目強制セレクトを有効化したばかりです。これをハンバーガーボタン等で開閉できるスライド式サイドメニューにするのは、`transform: translateX()` のトグルで比較的簡単に実現できます。

実装時に合わせて考えたいのが「本番公開時にデバッグ機能を一般プレイヤーに見せない」設計です。例えば `location.search` に `?debug=1` が無い限りデバッグセクションを表示しない、といったガードを入れておくと、オプション設定（ログ速度・音量など）とデバッグ専用機能（ダイス固定など）を安全に同居させられます。

### 6. キャラ選択などボタンのホバー時の動き

**難易度**: 🟢 低　**効果**: 🟡 中

`.dice-button:hover`・`#turn-decide-button:hover` は既に色反転の実装があるので、そのパターンを流用できます。`.choice-button`・`#decide-button`・`.nakamu5` などに `transition: transform 0.15s ease, box-shadow 0.15s ease;` を足し、`:hover` で `transform: translateY(-3px) scale(1.05);` や光る `box-shadow` を付けるだけで体感の良さが大きく変わります。実装コストの割にリターンが大きい項目です。

### 7. キャラクター攻撃時のアニメーション（右/左に少し動いて戻る）

**難易度**: 🟢 低〜🟡 中　**効果**: 🔴 高

前回のバグ修正で `applyDamage()` という**全キャラ・全ダメージ処理が必ず通る単一の窓口関数**を作ったばかりなので、ここに1箇所フックを足すだけで攻撃モーションを全技に一括適用できます。実装は既存の `damageEffect()`（`shake`クラス付与、`gameContainer.classList.add('shake')`）と全く同じパターンで作れます。

```css
@keyframes lungeRight { 30% { transform: translateX(30px); } 100% { transform: translateX(0); } }
@keyframes lungeLeft  { 30% { transform: translateX(-30px); } 100% { transform: translateX(0); } }
```

`applyDamage()` 内で「攻撃した側」（`filterDamage`に渡していない方、つまり呼び出し元の `actor`）のカード/アイコン要素に、player側なら `lungeRight`、enemy側なら `lungeLeft` のクラスを付けて `setTimeout` で外す、という形になります。

---

## B. 追加提案

### ゲーム機能

- **ダメージ数値のポップアップ表示**（🟢低 / 🔴高）：「-40」のような数字がキャラの上にふわっと浮かんで消える、RPGでおなじみの演出。`applyDamage()`にフックを1つ足すだけで全ダメージに適用できる。ログのテキストだけより直感的に伝わる。
- **HPゲージ（バー）表示**（🟢低 / 🟡中）：現状 `HP: 170` のテキストのみ。横長のバーにして、残量に応じて緑→黄→赤に色が変わるようにすると視認性が大きく上がる。`displayHPandSP()`の1箇所を直せば全キャラに適用される。
- **対戦成績・セーブ機能**（🟡中 / 🟡中）：現状セーブ機能が一切無い（ページを閉じたら何も残らない）。`localStorage`にキャラ別の勝敗数を記録するだけでも、繰り返し遊ぶ動機になる。
- **効果音・BGM**（🟡中 / 🔴高）：現状サウンドが一切無い。ダイスを回す音、ダメージ音、勝利ファンファーレだけでも体感がかなり変わる。素材はフリー音源を使うか、Web Audio APIで簡易的な効果音を生成する方法もある（ライセンス管理も忘れずに）。
- **タイトル画面**（🟡中 / 🟡中）：`index.html`に `<div id="start-display"></div>` という**空のプレースホルダーが既に存在**している（CSS・JSどちらにも紐付いていない）。ロゴ表示＋「はじめる」ボタンなどのタイトル画面を作る想定だったものが未着手のまま残っていると思われる。ここを活用すると自然にゲーム開始前の導線ができる。
- **キャラクター図鑑・遊び方画面**（🟡中 / 🟡中）：現状、各キャラの6つの技はキャラ選択画面でホバーしたときの画像でしか見られない。対戦前にまとめて確認できる図鑑・ヘルプ画面があると初見のハードルが下がる。
- **バトルログの蓄積表示**（🟢低 / 🟢低）：現状 `log()` は表示→クリアの繰り返しで、少し前のログは消えてしまう。ログを下に積み上げて後から見返せるようにすると、大きな戦闘の流れを追いやすくなる（デバッグ時にも便利）。

### 演出・UI

- **画面遷移のフェード**（🟢低 / 🟢低）：キャラ選択画面↔バトル画面の切り替えが現状 `display:none/block` で瞬間的。`opacity`のtransitionを挟むだけで印象が良くなる。
- **ダイスの3D風回転演出**（🟡中 / 🟡中）：現状は画像の差し替えのみでダイスを表現している。`transform: rotate3d()`などを使った擬似的な回転演出を加えると、より「サイコロを振っている」感が出る。
- **キャラクターのアイドルモーション**（🟢低 / 🟢低）：立ち絵にごくわずかな上下の揺れ（`translateY`の往復アニメーション）を常時つけると、静止画特有の硬さが和らぐ。効果は控えめだが実装は非常に軽い。

### 技術・運用

- **デバッグ機能の本番非表示化**（🟢低 / 🟡中）：左サイドメニュー化（A-5）と合わせて、`?debug=1`等でのゲートを入れておくと、一般公開後に誤ってダイス固定機能を使われる事故を防げる。
- **ファイル分割（保守性向上）**：前回の設計改善提案でも触れた内容。CPU対戦・エフェクト拡張などこれから機能を足していくほど、`script.js`（現在も2000行超）が肥大化して見通しが悪くなる。状態管理／UI描画／キャラクターアクション／ダイス処理などに分割しておくと、以降の「バイブコーディング」がやりやすくなる。
- **Playwrightでの簡易自動テストの継続活用**：前回のバグ修正確認で、ローカルサーバー＋headless Chromiumで「特定の出目を強制して狙った状況を再現する」検証ができることを確認済み。新機能を追加するたびに同様の手法でスモークテストしておくと、回帰バグの早期発見につながる。

---

## C. 進める順番の目安

強制ではありませんが、次のような順番がおすすめです。

1. **低難易度・高効果のものから**：⑥ホバー演出 → ⑦攻撃アニメーション → ダメージポップアップ・HPゲージ表示。いずれも`applyDamage()`など既存の共通処理にフックを足すだけで済み、見た目の変化が大きい。
2. **素材依存の解消**：③霧エフェクトのCSS化 → ④他キャラへの汎用化。①③④の並びで進めると、状態演出の共通基盤が育っていく。
3. **UI基盤の拡張**：⑤左サイドメニュー（オプション・デバッグ）。この段階で本番/デバッグの切り分けも一緒に設計しておくと後が楽。
4. **規模の大きい機能**：①CPU対戦。骨組みは既にあるので、意思決定ロジックとUIトグルの追加に集中できる。
5. **最後に基盤変更を伴うもの**：②スマホ対応・PWA化。スケール表示ラッパー方式なら他の変更を先に済ませてから着手しても手戻りが少ない。
